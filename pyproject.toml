[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atomforge"
version = "0.0.1"
description = "Atomspace reasoning graph"

[tool.setuptools]
packages = ["atomforge"]

